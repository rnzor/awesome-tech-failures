name: Sync Entries to NDJSON

on:
  push:
    branches:
      - main
    paths:
      - 'ai-slop-and-automation.md'
      - 'production-outages.md'
      - 'security-incidents.md'
      - 'startup-failures.md'
      - 'product-failures.md'
      - 'decision-failures.md'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install pyyaml

      - name: Run sync script
        run: python .github/sync_entries.py

      - name: Check for entry changes
        id: check
        run: |
          if git diff --quiet agent/entries.ndjson; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit entry changes
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add agent/entries.ndjson
          git commit -m "chore: auto-sync entries.ndjson from markdown files"
          git push

      - name: Regenerate embeddings
        if: steps.check.outputs.changed == 'true'
        run: python .github/generate_embeddings.py

      - name: Check for embedding changes
        if: steps.check.outputs.changed == 'true'
        id: check_embeddings
        run: |
          if git diff --quiet agent/embeddings.ndjson; then
            echo "embeddings_changed=false" >> $GITHUB_OUTPUT
          else
            echo "embeddings_changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit embeddings
        if: steps.check.outputs.changed == 'true' && steps.check_embeddings.outputs.embeddings_changed == 'true'
        run: |
          git add agent/embeddings.ndjson
          git commit -m "chore: auto-regenerate embeddings from entries"
          git push

